version: "3.5"

services:
  crawler:
    build: scripts
    command: ["python3", "fetch_all_data.py", "2019", "data"]    
    container_name: crawler_python
    volumes:
      - ./scripts:/code/scripts
      - ./data:/code/data